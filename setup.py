from setuptools import setup

setup(
    name="custom_func",
    version="0.1",
    description="A single script example",
    py_modules=["custom_func"],
    install_requires=[
        # List your dependencies here
        # e.g., 'numpy', 'requests'
    ],
)
